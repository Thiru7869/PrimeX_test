# Copy this file to ".env" and fill in real values.
# .env is git-ignored and never committed.

PROJECT_NAME=PrimeX AI
ENVIRONMENT=development
DEBUG=true

# Frontend origin allowed to call this API
CORS_ORIGINS=http://localhost:3000

# Database — leave blank for now, we set this up in the next prompt.
# For Neon it will look like:
# postgresql+asyncpg://USER:PASSWORD@HOST/DBNAME
DATABASE_URL=postgresql+asyncpg://neondb_owner:npg_ICXgN28uhbVU@ep-blue-math-atw1x9n2.c-9.us-east-1.aws.neon.tech/neondb