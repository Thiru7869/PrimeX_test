"use client";

export default function Home() {
  // AuthProvider will redirect to /chat or /login based on auth state.
  return (
    <div className="flex min-h-screen items-center justify-center text-gray-500">
      Redirecting…
    </div>
  );
}